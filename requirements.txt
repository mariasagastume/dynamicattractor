# Dynamic Attractor Network — Python dependencies
# The notebook uses only numpy + matplotlib; everything else it imports
# (os, glob, pickle, zipfile, time) is part of the Python standard library.
#
# Tested with Python 3.11–3.13.
#
# Reproducible install:
#   python -m venv .venv
#   source .venv/bin/activate      # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt

numpy>=1.24
matplotlib>=3.7

"""
EXTRA: Versions of all packages in virtual environment


contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
matplotlib==3.10.9
numpy==2.4.6
packaging==26.2
pathlib==1.0.1
pillow==12.2.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
"""